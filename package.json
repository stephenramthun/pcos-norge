{
  "name": "pcos-norge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "lint": "turbo lint",
    "test": "turbo test",
    "deploy": "turbo deploy",
    "clean": "rm -rf node_modules",
    "migrate:dev": "npm run migrate:dev -w db"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "rxjs": "^7.8.0",
    "turbo": "^1.8.1"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
