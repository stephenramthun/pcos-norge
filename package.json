{
  "name": "pcos-norge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "lint": "turbo lint",
    "test": "turbo test",
    "deploy": "turbo deploy",
    "clean": "rm -rf node_modules"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "rxjs": "^7.8.0",
    "turbo": "^1.8.1"
  }
}
